global g_tave g_tstd g_vave g_vstd g_nc g_step g_vmax g_runs g_run

run_init_vel = .1;
run_end_vel = 2;

run_init_nc = 20;
run_end_nc = 60;

run_init_tlc = 2;
run_end_tlc = 5;

g_vave = zeros(1, g_runs);
g_vstd = zeros(1, g_runs);

g_tave = zeros(1, g_runs);
g_tstd = zeros(1, g_runs);

dt_run_vel = 

for i=1:g_runs
    simulator(run_nc, run_tlc, 